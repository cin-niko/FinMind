{
  "workflow_id": "news-digest",
  "version": "1.0.0",
  "title": "News Digest",
  "description": "Trusted-source news and source document digest with freshness and citations.",
  "workflow_type": "atomic",
  "market_scope": ["VN_STOCK", "US_STOCK"],
  "required_inputs": [
    {"name": "market", "type": "string", "required": true},
    {"name": "symbol", "type": "string", "required": true}
  ],
  "required_datasets": ["source_documents"],
  "stages": ["data-collector", "data-quality-check", "news-digest"],
  "skill_refs": ["src/api/platform/workflows/skills/news-digest.md"],
  "output_sections": ["Data Quality", "News Digest"],
  "citation_policy": "material_claims_require_citations",
  "chart_requirements": [],
  "step_sequence": ["data-collector", "data-quality-check", "news-digest"]
}
