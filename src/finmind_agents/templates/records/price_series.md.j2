Price series record for {{ instrument_id }}.
- Interval: {{ payload.interval|default('Unavailable') }}
- Date range: {{ payload.start_date|default('Unavailable') }} to {{ payload.end_date|default('Unavailable') }}
- Bar count: {{ payload.count|default('Unavailable') }}
